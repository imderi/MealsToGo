import React from "react";
import styled from "styled-components/native";
import { AccountBackground } from "../components/account-background.styles";

const Text = styled.Text``;

export const Register = () => {
  return (
    <AccountBackground>
      <Text>Halo Register</Text>
    </AccountBackground>
  );
};
